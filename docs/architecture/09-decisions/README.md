# 架构决策

## 状态

- 当前状态：项目级 ADR 目录入口说明
- 用途：在项目级和模块级文档迁移正式开始后，存放长期有效的决策记录

## 已确认的治理决策主题

当前已经明确、后续可按需沉淀为 ADR 的主题包括：

- 当前正式 T05 模块为 `t05_topology_between_rc_v2`
- legacy `t05_topology_between_rc` 为历史参考模块
- `t03_marking_entity` 已退役
- `t10` 已退役
- repo root `AGENTS.md` 的职责边界
- `AGENTS.md` 与 `SKILL.md` 的最终收口规则

## 后续预期用法

未来的决策记录至少应包含：

- 决策本身
- 背景与备选方案
- 后果
- 受影响的模块与文档面
