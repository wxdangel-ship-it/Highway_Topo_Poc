# 01 引言与目标

## 状态

- 草案状态：Round 1 最小可信草案
- 来源依据：
  - `SPEC.md`
  - `docs/PROJECT_BRIEF.md`
  - `docs/codebase-research.md`
- 审核重点：
  - 确认项目目标表述
  - 确认模块 taxonomy 覆盖范围
  - 确认本文件未来应成为稳定摘要，而不是新的临时 brief

## 系统目标

`Highway_Topo_Poc` 的目标是以“双环境闭环”的方式验证并逐步加固高速道路拓扑生成仓库：

- 外网环境：工程开发、回归、文档整理、分析
- 内网环境：真实数据执行与仅文本化 QC 回传

仓库围绕模块化能力块组织，包括数据合成、质量控制、锚点生成、拓扑生成，以及 patch 级预处理/后处理。

## 业务目标

长期业务目标不只是“画出道路”，而是建立一个满足以下要求的闭环：

- 可运行
- 可诊断
- 可审核
- 可追溯
- 适应内外网之间仅文本结果传递的约束

## 文档目标

Round 1 同时引入一个并行的文档治理目标：

- 把项目级源事实与模块级源事实分离
- 让 `AGENTS.md` 保持小而稳定
- 让 `SKILL.md` 聚焦可复用工作流
- 为长期文档建立 arc42 风格的落位结构

## 范围说明

当前仓库现实包括：

- 以 `t00-t07` 为中心的正式项目 taxonomy
- 独立存在于仓库中的 `t05_topology_between_rc_v2` 与 `t10`
- 缺失的 taxonomy 成员 `t03_marking_entity`

这些不一致本身就是当前架构问题的一部分，因此在 Round 1 中应被显式暴露，而不是被隐藏。

## 待确认问题

- 未来项目级架构文档是否仍以 `SPEC.md` 为主要范围来源，还是最终需要在 `SPEC.md` 与 `docs/architecture/` 之间进一步拆分？
- 当 legacy T05 与 T05-V2 同时活跃时，项目级文档应如何呈现 T05 family？
