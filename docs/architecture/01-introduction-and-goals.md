# 01 引言与目标

## 状态

- 当前状态：项目级架构摘要说明
- 来源依据：
  - `SPEC.md`
  - `docs/PROJECT_BRIEF.md`
  - `docs/archive/nonstandard/codebase-research.md`
- 审核重点：
  - 确认项目目标表述
  - 确认正式模块与历史参考模块边界
- 确认本文件保持稳定摘要定位，而不是临时说明

## 系统目标

`Highway_Topo_Poc` 的目标是以“双环境闭环”的方式验证并逐步加固高速道路拓扑生成仓库：

- 外网环境：工程开发、回归、文档整理、分析
- 内网环境：真实数据执行与仅文本化 QC 回传

仓库围绕模块化能力块组织，包括数据合成、质量控制、锚点生成、拓扑生成，以及 patch 级预处理 / 后处理。

## 业务目标

长期业务目标不只是“画出道路”，而是建立一个满足以下要求的闭环：

- 可运行
- 可诊断
- 可审核
- 可追溯
- 适应内外网之间仅文本结果传递的约束

## 文档目标

当前文档治理目标是：

- 把项目级源事实与模块级源事实分离
- 让 `AGENTS.md` 保持小而稳定
- 让 `SKILL.md` 聚焦可复用工作流
- 为长期文档建立 arc42 风格的落位结构

## 范围说明

当前项目级正式口径为：

- 当前活跃模块：`t04`、当前正式 T05（即 `t05_topology_between_rc_v2`）、`t06`
- 历史参考模块：legacy `t05_topology_between_rc`
- 已退役模块：`t02`、`t03_marking_entity`、`t07`、`t10`
- 仓库保留的支撑 / 测试模块：`t00`、`t01`

这些分类用于当前文档治理分层，不代表所有模块都处于同一种维护深度。

## 当前无待确认项

当前模块身份与治理入口口径已经写回项目级文档。
