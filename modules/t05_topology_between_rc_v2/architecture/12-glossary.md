# T05-V2 术语表

- **Segment**：语义边界之间的早期候选拓扑跨度。
- **CorridorWitness**：用于支持 corridor 判定的证据。
- **CorridorIdentity**：在 slot 映射前解析出的 corridor 含义。
- **Slot**：最终 road 生成前的端点落位构造。
- **FinalRoad**：模块最终输出的有向道路几何。
